function main()

    local a = io.read("n")
    local b = io.read("n")
    local c = io.read("n")
    local d = io.read("n")

    dif = (a * b - c * d)
    print("DIFERENCA = " .. dif)
    
end

main()
